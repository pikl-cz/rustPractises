Spuštění example Rust kódu:

```bash

$ rustc main.rs
$ ./main
Hello, world!

```

# Správa projektu

```bash
cargo new new_project
cd new_project
cargo run
cargo build
cargo install --path . // instalace binárky lokálně
cargo test
cargo fmt // formátování kódu
cargo clippy // linting
```
